# Dependencies
node_modules/
.venv/
venv/
*.pyc
__pycache__/

# Environment (dotenvx)
# .env files ARE committed (encrypted)
# .env.keys is NOT (contains private decryption keys)
.env.keys
.env.local

# Next.js
.next/
out/

# Build
dist/
build/
coverage/

# IDE
.vscode/
.idea/

# Local agent/skill installs (pinned by skills-lock.json; reinstall on clone)
.claude/skills/
.claude/settings.local.json
.agents/skills/

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Uploads
uploads/

# Trace harness — per-run extraction artifacts (Chandra raw, rendered
# page PNGs, DoclingDocument JSON, LLM I/O, timings). Reproducible via
# `_tracer.py` inside the backend container; not source-of-truth.
local-shared-data/traces/
